import {NextRequest,NextResponse} from 'next/server';import {requireBrowserKey,randomToken} from '@/lib/security';import {googleAuthUrl} from '@/lib/google';
export async function GET(req:NextRequest){if(!requireBrowserKey(req))return NextResponse.json({error:'Yetkisiz'},{status:401});if(!process.env.GOOGLE_CLIENT_ID||!process.env.GOOGLE_CLIENT_SECRET||!process.env.GOOGLE_REDIRECT_URI)return NextResponse.json({error:'Google OAuth env eksik'},{status:400});const state=randomToken(24);const r=NextResponse.json({url:googleAuthUrl(state)});r.cookies.set('jarvis_google_state',state,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:600,path:'/'});return r;}
