import {NextRequest,NextResponse} from 'next/server';import {requireBrowserKey} from '@/lib/security';
export async function POST(req:NextRequest){if(!requireBrowserKey(req))return NextResponse.json({error:'Yetkisiz'},{status:401});const fd=await req.formData();const file=fd.get('file');if(!(file instanceof File))return NextResponse.json({error:'Ses dosyası gerekli'},{status:400});const out=new FormData();out.append('file',file,file.name||'voice.webm');out.append('model',process.env.OPENAI_TRANSCRIBE_MODEL||'gpt-4o-mini-transcribe');out.append('language','tr');const r=await fetch('https://api.openai.com/v1/audio/transcriptions',{method:'POST',headers:{authorization:`Bearer ${process.env.OPENAI_API_KEY}`},body:out});const d=await r.json().catch(()=>({}));if(!r.ok)return NextResponse.json({error:d?.error?.message||'Transkripsiyon başarısız'},{status:r.status});return NextResponse.json({text:d.text||''});}
