import {NextRequest,NextResponse} from 'next/server';import {requireBrowserKey} from '@/lib/security';
export async function POST(req:NextRequest){if(!requireBrowserKey(req))return NextResponse.json({error:'Yetkisiz'},{status:401});const b=await req.json();const input=String(b.text||'').trim().slice(0,3500);if(!input)return NextResponse.json({error:'Metin gerekli'},{status:400});const r=await fetch('https://api.openai.com/v1/audio/speech',{method:'POST',headers:{authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'content-type':'application/json'},body:JSON.stringify({model:process.env.OPENAI_TTS_MODEL||'gpt-4o-mini-tts',voice:process.env.OPENAI_TTS_VOICE||'alloy',input,format:'mp3'})});if(!r.ok){const e=await r.text();return NextResponse.json({error:e},{status:r.status});}return new NextResponse(await r.arrayBuffer(),{headers:{'content-type':'audio/mpeg','cache-control':'no-store'}});}
